     declare

	   priority_queue_$create entry returns (pointer),
	   priority_queue_$destroy entry (pointer),
	   priority_queue_$count  entry (pointer) returns (fixed bin (36)),
	   priority_queue_$enqueue entry (pointer, pointer, entry) returns (pointer),
	   priority_queue_$dequeue entry (pointer) returns (pointer),
	   priority_queue_$front  entry (pointer) returns (pointer),
	   priority_queue_$back   entry (pointer) returns (pointer);

